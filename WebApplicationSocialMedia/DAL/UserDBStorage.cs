using WebApplicationSocialMedia.Models;
using WebApplicationSocialMedia.Services;

namespace WebApplicationSocialMedia.DAL
{
    public class SocialMediaDBStorage
    {
        //private readonly ApplicationDbContext _context;

        //public SocialMediaDBStorage(ApplicationDbContext socialMediaContext)
        //{
        //    _context = socialMediaContext;
        //}
        //public void AddUser(User user)
        //{
        //    _context.Users.Add(user);
        //    _context.SaveChanges();
        //}
        //public User? GetUser(string id)
        //{
        //    User? user = _context.Users.FirstOrDefault(x => x.userID == id);
        //    return user;
        //}
        //public List<User> GetAllUsers()
        //{
        //    List<User> user = _context.Users.ToList();
        //    return user;
        //}
        //public List<User> GetUsers(string? Name)
        //{
        //    List<User> user;
        //    user = _context.Users.Where(x => x.name == Name).ToList();
        //    return user;
        //}
        //public void RemoveUser(User user)
        //{
        //    _context.Users.Remove(user);
        //    _context.SaveChanges();
        //}
        //public void RemoveUser(string? userName)
        //{
        //    _context.Users.RemoveRange(GetUsers(userName));
        //    _context.SaveChanges();
        //}

    }

}
